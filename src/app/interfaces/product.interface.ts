export default interface Product {
  availableColors: Array<string>,
  productBadgeString: string,
  productColor: string,
  productId: string,
  productName: string,
  productPrice: number,
  productStyle: string,
  quantity: string,
  swatchImage: string,
  thumbnails: object
}
